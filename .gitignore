# OS
.DS_Store

# Vercel CLI link/state, created by `vercel link` / `vercel dev` in any
# service directory.
.vercel

# Never commit real secrets - only the .env.example templates are tracked.
.env
.env.local
.env.*.local

# Local D1 emulator's sqlite file (backend/scripts/local-d1-server.ts).
/database
