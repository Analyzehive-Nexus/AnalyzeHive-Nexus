# Base URL of the Express backend. Baked into the client bundle at build
# time (NEXT_PUBLIC_ prefix), so changing it on Vercel requires a redeploy.
# Local:      http://localhost:8000
# Production: https://<your-backend-project>.vercel.app
NEXT_PUBLIC_API_URL=http://localhost:8000
